model:
  dropout: 0.2
  num_heads: 8
  num_layers: 1
  backbone: "ViT-B/16"
  dataset_name: "CUB-200-2011"
  
checkpoint:
  enabled: true
  cache_dir: "checkpoints/vife"

training:
  epochs: 10
  batch_size: 8
  mode: "logits"
  device: "cuda:0"
  precision: "fp32"
  optimizer: "SGD"
  use_cutout: false
  increment_epochs: 0
  learning_rate: 0.001
  weight_decay: 0.0005
  confusion_matrix: false
  visualize_gradcam: false
  class_distribution: false
  visualize_attention: false

data:
  kshot: 4
  seeds: [1, 10, 100, 1000, 10000]
  num_workers: 8
  run_eda: false
  clip_mean: [0.48145466, 0.4578275, 0.40821073]
  clip_std: [0.26862954, 0.26130258, 0.27577711]
  root: "datasets/cub-200-2011-renamed"
  base_novel:
    enabled: false
    split_ratio: 0.5

ssl:
  enabled: true
  enable_stage1: true
  enable_stage2: true
  enable_stage3: true
  default_fusion_weight: 0.5
  ssl_batch_size: 32
  eval_batch_size: 128
  proj_dim: 256
  num_prototypes: 4096
  ssl_epochs: 5
  ssl_lr: 0.0001
  teacher_temp: 0.04
  student_temp: 0.1
  ema_momentum: 0.996
  final_ema_momentum: 1.0
  center_momentum: 0.9
  eval_freq: 5
  eval_linear_epochs: 15
  linear_epochs: 5
  linear_lr: 0.001
  fusion_max_iter: 20
  fusion_lr: 0.001
  learn_fusion: true
  num_trans_layers: 1
  num_heads: 8
  global_crop_size: 224
  local_crop_size: 96
  num_local_crops: 4
  global_crop_scale_min: 0.4
  global_crop_scale_max: 1.0
  local_crop_scale_min: 0.05
  local_crop_scale_max: 0.4
  num_unlabeled: 500
  use_labeled_for_ssl: false
  num_plot: 8
  save_stage1_checkpoint: true

logging:
  output_dir: "outputs/vife"
